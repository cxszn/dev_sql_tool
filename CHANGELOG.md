# 更新记录

## 0.5.0 — 2026-09-24

首次发布 Windows x64 便携版。

- 支持静态 PHP migration 与 PostgreSQL 16 / 18 SQL 双向转换，提供桌面客户端、CLI 和 stdio MCP。
- 使用 `SqlStudio` 自有 PHP 命名空间；包含中文 IDE 声明、链式类型提示和可携带的 VS Code / Cursor 工作区。
- 模板页支持导出 IDE 提示包。MCP 提供 10 个工具、3 个资源和 2 个提示词，附带 sql 编写助手技能与智能体说明。
- 保留结构、注释、索引、外键、序列和静态初始化数据；SQL→PHP 写入前核对往返语义，每份 SQL 对应一份 PHP。
- 默认开启源文件与输出文件哈希检查；有意重新生成时保留备份。
- 修复浮点字段仅指定 `places` 命名参数时的解析错误。
- 便携包可在解压目录生成对应的 MCP 连接配置，无需外部 Node.js。

升级已有文件时，旧 Hyperf / Illuminate 导入仍可读取。新生成文件统一使用 SqlStudio；反向转换器版本变化会触发旧清单保护。软件仅生成文件，不执行 PHP 或数据库 SQL。
